/*typedef*/
#include<stdio.h>
#include<math.h>
typedef int units;
int main()
{
    units batch1,batch2;
    batch1=16;
    batch2=9;
    int a=sqrt(batch1+batch2);
    printf("The squareroot is : %d",a);
    return 0;
}